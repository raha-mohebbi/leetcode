Solved the Two Sum problem by iterating through the array and checking every possible pair of numbers.

Steps:
1. Took each number as the current value.
2. Compared it with the remaining numbers in the array.
3. Checked if the sum of two numbers equals the target.
4. Returned the indices of the two numbers when a valid pair was found.

This approach uses a nested loop with O(n²) time complexity.